# Phase 17: Dashboard Refinement — Part 6: Ops & Verification
> Split from `017-phase-17-dashboard-refinement.md`. See other parts in this directory.

## Implementation Order

1. Dashboard layout + sidebar
2. Empty state component
3. Skeleton components
4. Error boundary
5. Toast provider + query error handling
6. Loading button
7. Page header + breadcrumb
8. Dashboard home page
9. Settings index page
10. Apply layout to all routes
11. Add loading/empty states to existing pages
12. Test error handling flows

---


## Verification

1. Navigate through all sections via sidebar
2. Active state shows current page
3. Empty states appear when no data
4. Loading skeletons during data fetch
5. Errors show toast + retry option
6. Dashboard home shows overview
7. All pages consistent layout

---


## Decisions

1. **Sidebar width**: 256px (w-64)
2. **Max content width**: 1152px (max-w-6xl)
3. **Toast position**: bottom-right
4. **Error boundary**: Per-route, not global
5. **Loading strategy**: Skeletons for known shapes, spinners for buttons

---


## Resolved Questions

All questions resolved during design.

---


## Implementation Readiness Review

> Auto-generated 2026-02-10

### Action Items

| Pri | Action | Verify |
|-----|--------|--------|
| ~~P0~~ | ~~Route structure mismatch~~ → RESOLVED: use `/dashboard` prefix | — |
| P0 | Implement data layer hooks (portfolio, agent, journal queries) | Hooks return data |
| P1 | Fix theme violations (hardcoded `text-green-500` -> CSS vars) | No hardcoded Tailwind colors |
| P1 | Add missing components (EmptyState, Skeleton, ErrorBoundary) | Components render correctly |
| P2 | Verify `sonner` toast package installed | Package in dependencies |

### Resolution: Route Structure

**Decision**: Use `/dashboard` pattern (not `/_auth/app/`). Refactor existing routes:
- `/_auth/app/` → `/dashboard`
- Auth-protected routes live under `/dashboard/*`
- Settings pages: `/dashboard/settings/*`
- Portfolio: `/dashboard/portfolio`
- Signals: `/dashboard/signals`
- Journal: `/dashboard/journal`

Existing `/_auth/` prefix was an internal convention. `/dashboard` is user-facing and cleaner.

### Gaps

- Route structure: existing code uses `/_auth/app/` but doc proposes `/dashboard`, `/portfolio` etc - mismatch
- Missing data layer: `useAccount`, `usePositions`, `useAgentStatus`, `useJournalStats` hooks undefined
- Theme violations: hardcoded `text-green-500`, `text-red-500` violate CSS variable rules from ui.md
- Missing components: EmptyState, Skeleton, ErrorBoundary, LoadingButton, PageHeader, Breadcrumb
- Mobile responsive listed as "Post-MVP" but no viewport/breakpoint strategy
- How ErrorBoundary integrates with TanStack Router built-in error boundaries unclear

