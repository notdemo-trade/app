# Phase 4: Signal Gathering — Part 2: Data Model
> Split from `004-phase-4-signal-gathering.md`. See other parts in this directory.

## Database Schema

```sql
-- packages/data-ops/migrations/0004_signal_gathering.sql

-- User watchlists
CREATE TABLE watchlists (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL REFERENCES auth_user(id) ON DELETE CASCADE,
  symbol TEXT NOT NULL,
  added_at TIMESTAMP NOT NULL DEFAULT NOW(),
  UNIQUE(user_id, symbol)
);

CREATE INDEX idx_watchlists_user ON watchlists(user_id);

-- Raw events (audit trail, one per external message)
CREATE TABLE raw_events (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL REFERENCES auth_user(id) ON DELETE CASCADE,
  source TEXT NOT NULL,         -- 'stocktwits', 'reddit', etc.
  source_id TEXT NOT NULL,      -- external unique ID
  raw_content JSONB NOT NULL,   -- full payload
  ingested_at TIMESTAMP NOT NULL DEFAULT NOW(),
  UNIQUE(user_id, source, source_id)
);

CREATE INDEX idx_raw_events_user ON raw_events(user_id);
CREATE INDEX idx_raw_events_source ON raw_events(source);
CREATE INDEX idx_raw_events_ingested ON raw_events(ingested_at);

-- Structured signals (normalized, queryable)
CREATE TABLE signals (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL REFERENCES auth_user(id) ON DELETE CASCADE,
  raw_event_id UUID REFERENCES raw_events(id) ON DELETE SET NULL,
  source TEXT NOT NULL,
  symbol TEXT NOT NULL,
  signal_type TEXT NOT NULL,    -- 'sentiment', 'trending', 'mention'
  direction TEXT NOT NULL,       -- 'bullish', 'bearish', 'neutral'
  strength DECIMAL(3,2) NOT NULL CHECK (strength >= 0 AND strength <= 1),
  summary TEXT,
  metadata JSONB,               -- source-specific data
  created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_signals_user ON signals(user_id);
CREATE INDEX idx_signals_symbol ON signals(symbol);
CREATE INDEX idx_signals_created ON signals(created_at DESC);
CREATE INDEX idx_signals_user_symbol ON signals(user_id, symbol);

-- Ingestion state tracking
CREATE TABLE ingestion_state (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL REFERENCES auth_user(id) ON DELETE CASCADE,
  source TEXT NOT NULL,
  last_poll_at TIMESTAMP,
  last_event_id TEXT,           -- cursor for incremental polling
  error_count INT DEFAULT 0,
  last_error TEXT,
  UNIQUE(user_id, source)
);

CREATE INDEX idx_ingestion_state_user ON ingestion_state(user_id);
```

### Drizzle Schema

```ts
// packages/data-ops/src/drizzle/schema.ts

export const watchlists = pgTable("watchlists", {
  id: uuid("id").defaultRandom().primaryKey(),
  userId: text("user_id")
    .notNull()
    .references(() => auth_user.id, { onDelete: "cascade" }),
  symbol: text("symbol").notNull(),
  addedAt: timestamp("added_at").defaultNow().notNull(),
}, (table) => ({
  uniqueUserSymbol: unique().on(table.userId, table.symbol),
}))

export const rawEvents = pgTable("raw_events", {
  id: uuid("id").defaultRandom().primaryKey(),
  userId: text("user_id")
    .notNull()
    .references(() => auth_user.id, { onDelete: "cascade" }),
  source: text("source").notNull(),
  sourceId: text("source_id").notNull(),
  rawContent: jsonb("raw_content").notNull(),
  ingestedAt: timestamp("ingested_at").defaultNow().notNull(),
}, (table) => ({
  uniqueUserSourceId: unique().on(table.userId, table.source, table.sourceId),
}))

export const signals = pgTable("signals", {
  id: uuid("id").defaultRandom().primaryKey(),
  userId: text("user_id")
    .notNull()
    .references(() => auth_user.id, { onDelete: "cascade" }),
  rawEventId: uuid("raw_event_id")
    .references(() => rawEvents.id, { onDelete: "set null" }),
  source: text("source").notNull(),
  symbol: text("symbol").notNull(),
  signalType: text("signal_type").notNull(),
  direction: text("direction").notNull(),
  strength: decimal("strength", { precision: 3, scale: 2 }).notNull(),
  summary: text("summary"),
  metadata: jsonb("metadata"),
  createdAt: timestamp("created_at").defaultNow().notNull(),
})

export const ingestionState = pgTable("ingestion_state", {
  id: uuid("id").defaultRandom().primaryKey(),
  userId: text("user_id")
    .notNull()
    .references(() => auth_user.id, { onDelete: "cascade" }),
  source: text("source").notNull(),
  lastPollAt: timestamp("last_poll_at"),
  lastEventId: text("last_event_id"),
  errorCount: integer("error_count").default(0),
  lastError: text("last_error"),
}, (table) => ({
  uniqueUserSource: unique().on(table.userId, table.source),
}))
```

---


## Database Performance & Migrations

### Missing Indexes (P0 Blocker)

Add these indexes for query performance:

```sql
-- Add to migration 0004 or create new migration 0004b
CREATE INDEX idx_signals_created_at ON signals(created_at DESC);
```

### Migration Verification

Before deploying, verify all migrations applied:

```bash
# scripts/verify-migrations.sh
export PGHOST="$DATABASE_HOST"
export PGUSER="$DATABASE_USERNAME"
export PGPASSWORD="$DATABASE_PASSWORD"
export PGDATABASE="neondb"

APPLIED=$(psql -t -c "SELECT version FROM pg_migrations ORDER BY version;")
EXPECTED=$(ls migrations/*.sql | wc -l)
if [ "$APPLIED" -ne "$EXPECTED" ]; then
  echo "ERROR: Only $APPLIED/$EXPECTED migrations applied"
  exit 1
fi
```

Add to CI/CD before deploy step.

---


## Zod Schemas

```ts
// packages/data-ops/src/zod-schema/signals.ts

import { z } from "zod"

export const SignalSourceSchema = z.enum(["stocktwits", "reddit", "twitter", "sec"])
export const SignalDirectionSchema = z.enum(["bullish", "bearish", "neutral"])
export const SignalTypeSchema = z.enum(["sentiment", "trending", "mention", "insider", "filing", "influencer"])

export const SignalSchema = z.object({
  id: z.string().uuid(),
  userId: z.string(),
  rawEventId: z.string().uuid().nullable(),
  source: SignalSourceSchema,
  symbol: z.string(),
  signalType: SignalTypeSchema,
  direction: SignalDirectionSchema,
  strength: z.number().min(0).max(1),
  summary: z.string().nullable(),
  metadata: z.record(z.unknown()).nullable(),
  createdAt: z.date(),
})

export const WatchlistItemSchema = z.object({
  id: z.string().uuid(),
  userId: z.string(),
  symbol: z.string(),
  addedAt: z.date(),
})

export const AddToWatchlistRequestSchema = z.object({
  symbol: z.string().min(1).max(10).transform(s => s.toUpperCase().trim()),
})

export const RemoveFromWatchlistRequestSchema = z.object({
  symbol: z.string().min(1).max(10).transform(s => s.toUpperCase().trim()),
})

export const GetSignalsRequestSchema = z.object({
  symbol: z.string().optional(),
  limit: z.coerce.number().int().min(1).max(100).optional().default(50),
  since: z.coerce.date().optional(),
})

export type Signal = z.infer<typeof SignalSchema>
export type WatchlistItem = z.infer<typeof WatchlistItemSchema>
```

---

