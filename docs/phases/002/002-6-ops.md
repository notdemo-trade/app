# Phase 2: Credentials Configuration — Part 6: Ops & Verification
> Split from `002-phase-2-credentials-configuration.md`. See other parts in this directory.

## Implementation Order

1. Encryption module (`crypto/credentials.ts`)
2. Database migrations
3. Drizzle schema
4. Zod schemas
5. Query functions
6. Validation service
7. API handlers (data-service)
8. Server functions (user-application)
9. UI components
10. Integration testing

---


## Verification

1. Go to Settings > Credentials
2. Add Alpaca keys (paper mode) - verify "Connected" badge
3. Add OpenAI key - verify validation passes
4. Go to Settings > Trading
5. Configure max positions = 5, stop loss = 10%
6. Verify model dropdowns show only configured providers
7. Test API endpoint with bearer token returns user's config

**Test invalid credentials**:
- Add wrong Alpaca key - verify "Invalid" badge
- Add expired LLM key - verify error message

---


## Security Considerations

- Master key stored in CF secrets, never in code
- Per-user key derivation limits blast radius
- IV unique per encryption (no reuse)
- Credentials never logged or returned in API responses
- Session auth required (not bearer token) for credential management
- Validation results stored, raw errors sanitized

---


## Alternatives Considered

### Encryption: AES-256-GCM vs AES-256-CBC
- **GCM (chosen)**: authenticated encryption, detects tampering
- **CBC**: requires separate HMAC

### Key derivation: HKDF vs PBKDF2
- **HKDF (chosen)**: faster, designed for key derivation from strong keys
- **PBKDF2**: slower, designed for passwords

### Storage: DB vs KV
- **DB (chosen)**: transactional, consistent, relational to user
- **KV**: faster reads but eventually consistent, no joins

---


## Decisions

1. **Credential expiration reminder**: Yes, show warning on dashboard 3 days before credential expires (for providers with expiring keys)
2. **Rate limit validation**: Skip for now
3. **Cache decrypted creds**: Yes, cache in Durable Object memory during agent run (decrypt once per session)
4. **Custom OpenAI base URLs**: Yes, support for proxies/self-hosted (add `baseUrl` field to LLM credential schema)

---


## Implementation Readiness Review

> Auto-generated 2026-02-10

### Action Items

| Pri | Action | Verify |
|-----|--------|--------|
| P0 | Fix Twitter/X validation endpoint (needs OAuth2, not app-only bearer) | Validation call succeeds with valid creds |
| P0 | Add `onConflictDoUpdate` target: `[table.userId, table.provider]` | Upsert test passes |
| P1 | Use `baseUrl` field in LLM validation service | Custom endpoint validation works |
| P1 | Add credential update flow (currently only create/delete) | PATCH endpoint exists |
| P2 | Document multi-provider UX (switch active LLM provider) | UI shows provider selector |

### Gaps

- Symbol validation cache uses in-memory Map, resets on Worker restart (~15min) - use KV
- Paper/live toggle impact on existing validation status undefined
- Config bounds have Zod min/max but no UI hints on reasonable vs extreme
- Credential rotation mechanism missing (only create/delete)

