# Phase 5: Technical Analysis Agent — Part 2: Data Model

## Agent SQLite State

TechnicalAnalysisAgent uses `this.sql` to cache bars and computed indicators. Avoids re-fetching from Alpaca on every RPC call.

```sql
-- Initialized in onStart()
CREATE TABLE IF NOT EXISTS bars (
  timestamp TEXT PRIMARY KEY,
  open REAL NOT NULL,
  high REAL NOT NULL,
  low REAL NOT NULL,
  close REAL NOT NULL,
  volume INTEGER NOT NULL,
  trade_count INTEGER,
  vwap REAL
);

CREATE TABLE IF NOT EXISTS indicators (
  key TEXT PRIMARY KEY DEFAULT 'latest',
  data TEXT NOT NULL,            -- JSON serialized TechnicalIndicators
  computed_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS detected_signals (
  id TEXT PRIMARY KEY,
  type TEXT NOT NULL,
  direction TEXT NOT NULL,
  strength REAL NOT NULL,        -- stored as REAL, read as number
  description TEXT NOT NULL,
  detected_at TEXT NOT NULL
);
```

## Shared PG Schema

### signals table (extracted from Phase 4)

Phase 4 defines the full signal infra (raw_events, ingestion_state, watchlists). Phase 5 only needs the `signals` table to write TA results. Extracted here as a prereq.

```sql
-- packages/data-ops/migrations/0005_signals.sql

CREATE TABLE signals (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  source_agent TEXT NOT NULL,    -- 'technical_analysis', 'stocktwits', etc.
  symbol TEXT,
  series_id TEXT,
  signal_type TEXT NOT NULL,
  direction TEXT NOT NULL,       -- 'bullish' | 'bearish' | 'neutral'
  strength NUMERIC(3,2) NOT NULL CHECK (strength >= 0 AND strength <= 1),
  summary TEXT,
  metadata JSONB,
  raw_event_id UUID,             -- FK added in Phase 4 when raw_events table ships
  created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_signals_source ON signals(source_agent);
CREATE INDEX idx_signals_symbol ON signals(symbol);
CREATE INDEX idx_signals_created ON signals(created_at DESC);
CREATE INDEX idx_signals_symbol_created ON signals(symbol, created_at DESC);
```

### api_credentials table (minimal BYOK prereq)

Stores encrypted Alpaca keys per user. Full BYOK management UI ships in Phase 2.

```sql
-- packages/data-ops/migrations/0005_api_credentials.sql

CREATE TABLE api_credentials (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL REFERENCES auth_user(id) ON DELETE CASCADE,
  provider TEXT NOT NULL,          -- 'alpaca'
  encrypted_key TEXT NOT NULL,
  encrypted_secret TEXT NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW(),
  UNIQUE(user_id, provider)
);
```

### Drizzle Schema

```ts
// packages/data-ops/src/drizzle/schema.ts

import { pgTable, uuid, text, timestamp, decimal, jsonb, unique, index } from "drizzle-orm/pg-core"
import { auth_user } from "./auth-schema"

export const signals = pgTable("signals", {
  id: uuid("id").defaultRandom().primaryKey(),
  sourceAgent: text("source_agent").notNull(),
  symbol: text("symbol"),
  seriesId: text("series_id"),
  signalType: text("signal_type").notNull(),
  direction: text("direction").notNull(),
  strength: decimal("strength", { precision: 3, scale: 2 }).notNull(),
  summary: text("summary"),
  metadata: jsonb("metadata"),
  rawEventId: uuid("raw_event_id"),  // FK added when Phase 4 ships
  createdAt: timestamp("created_at").defaultNow().notNull(),
})

export const apiCredentials = pgTable("api_credentials", {
  id: uuid("id").defaultRandom().primaryKey(),
  userId: text("user_id")
    .notNull()
    .references(() => auth_user.id, { onDelete: "cascade" }),
  provider: text("provider").notNull(),
  encryptedKey: text("encrypted_key").notNull(),
  encryptedSecret: text("encrypted_secret").notNull(),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
}, (table) => ({
  uniqueUserProvider: unique().on(table.userId, table.provider),
}))
```

### Query Functions

```ts
// packages/data-ops/src/queries/signals.ts

import { getDb } from "../database/setup"
import { signals } from "../drizzle/schema"
import { desc, eq, and, gte } from "drizzle-orm"

interface InsertSignalInput {
  sourceAgent: string
  symbol: string
  signalType: string
  direction: string
  strength: number
  summary: string
  metadata?: Record<string, unknown>
}

export async function insertSignal(input: InsertSignalInput) {
  const db = getDb()
  const [row] = await db.insert(signals).values({
    sourceAgent: input.sourceAgent,
    symbol: input.symbol,
    signalType: input.signalType,
    direction: input.direction,
    strength: input.strength.toFixed(2),
    summary: input.summary,
    metadata: input.metadata ?? null,
  }).returning()
  return row
}

export async function getSignalsBySymbol(symbol: string, limit = 50) {
  const db = getDb()
  return db.select().from(signals)
    .where(eq(signals.symbol, symbol))
    .orderBy(desc(signals.createdAt))
    .limit(limit)
}

export async function getSignalsSince(since: Date, symbol?: string) {
  const db = getDb()
  const conditions = [gte(signals.createdAt, since)]
  if (symbol) conditions.push(eq(signals.symbol, symbol))
  return db.select().from(signals)
    .where(and(...conditions))
    .orderBy(desc(signals.createdAt))
}
```

```ts
// packages/data-ops/src/queries/credentials.ts

import { getDb } from "../database/setup"
import { apiCredentials } from "../drizzle/schema"
import { eq, and } from "drizzle-orm"

export async function getCredentials(userId: string, provider: string) {
  const db = getDb()
  const [row] = await db.select().from(apiCredentials)
    .where(and(
      eq(apiCredentials.userId, userId),
      eq(apiCredentials.provider, provider),
    ))
  return row ?? null
}
```

---

## TA Agent Signal Types

TA agent writes to `signals` with:
- `source_agent = 'technical_analysis'`
- `symbol = agent's symbol`
- Signal types: `rsi_oversold`, `rsi_overbought`, `macd_bullish`, `macd_bearish`, `bb_lower_touch`, `bb_upper_touch`, `golden_cross_active`, `death_cross_active`, `high_volume`

---

## Zod Schemas

```ts
// packages/data-ops/src/zod-schema/analysis.ts

import { z } from "zod"

export const TimeframeSchema = z.enum(["1Min", "5Min", "15Min", "1Hour", "1Day"])

export const BarSchema = z.object({
  t: z.string(),
  o: z.number(),
  h: z.number(),
  l: z.number(),
  c: z.number(),
  v: z.number(),
  n: z.number(),
  vw: z.number(),
})

export const MACDSchema = z.object({
  macd: z.number(),
  signal: z.number(),
  histogram: z.number(),
})

export const BollingerSchema = z.object({
  upper: z.number(),
  middle: z.number(),
  lower: z.number(),
  width: z.number(),
})

export const TechnicalIndicatorsSchema = z.object({
  symbol: z.string(),
  timestamp: z.string(),
  price: z.number(),
  sma_20: z.number().nullable(),
  sma_50: z.number().nullable(),
  sma_200: z.number().nullable(),
  ema_12: z.number().nullable(),
  ema_26: z.number().nullable(),
  rsi_14: z.number().nullable(),
  macd: MACDSchema.nullable(),
  bollinger: BollingerSchema.nullable(),
  atr_14: z.number().nullable(),
  volume_sma_20: z.number().nullable(),
  relative_volume: z.number().nullable(),
})

export const TechnicalSignalSchema = z.object({
  type: z.string(),
  direction: z.enum(["bullish", "bearish", "neutral"]),
  strength: z.number().min(0).max(1),
  description: z.string(),
})

export const AnalysisResultSchema = z.object({
  symbol: z.string(),
  timeframe: TimeframeSchema,
  indicators: TechnicalIndicatorsSchema,
  signals: z.array(TechnicalSignalSchema),
  bars: z.array(BarSchema),
})

export const GetAnalysisRequestSchema = z.object({
  symbol: z.string().min(1).max(10),
  timeframe: TimeframeSchema.optional().default("1Day"),
  includeBars: z.coerce.boolean().optional().default(true),
})

export const BatchAnalysisRequestSchema = z.object({
  symbols: z.array(z.string().min(1).max(10)).min(1).max(20),
  timeframe: TimeframeSchema.optional().default("1Day"),
})

export type Timeframe = z.infer<typeof TimeframeSchema>
export type Bar = z.infer<typeof BarSchema>
export type TechnicalIndicators = z.infer<typeof TechnicalIndicatorsSchema>
export type TechnicalSignal = z.infer<typeof TechnicalSignalSchema>
export type AnalysisResult = z.infer<typeof AnalysisResultSchema>
```

```ts
// packages/data-ops/src/zod-schema/signals.ts
// Note: SignalSourceSchema uses plain text() in Drizzle (no enum constraint)
// so new sources like "technical_analysis" don't require schema migration.
// Zod validation is per-context:

import { z } from "zod"

export const SignalDirectionSchema = z.enum(["bullish", "bearish", "neutral"])

export const SignalSchema = z.object({
  id: z.string().uuid(),
  sourceAgent: z.string(),           // open string, not enum — extensible
  symbol: z.string().nullable(),
  seriesId: z.string().nullable(),
  signalType: z.string(),
  direction: SignalDirectionSchema,
  strength: z.coerce.number().min(0).max(1),  // coerce: PG decimal → number
  summary: z.string().nullable(),
  metadata: z.record(z.unknown()).nullable(),
  createdAt: z.date(),
})

export const GetSignalsRequestSchema = z.object({
  symbol: z.string().optional(),
  source: z.string().optional(),
  limit: z.coerce.number().int().min(1).max(100).optional().default(50),
  since: z.coerce.date().optional(),
})

export type Signal = z.infer<typeof SignalSchema>
```

---
