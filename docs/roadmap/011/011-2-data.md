# Phase 11: Telegram Approvals — Part 2: Data Model

## Credential Schema Changes

Add `telegram` to the credential provider enum and define the credential shape.

```ts
// packages/data-ops/src/credential/schema.ts (additions)

// Add to CredentialProvider union:
export type CredentialProvider =
  | "alpaca" | "openai" | "anthropic" | "google" | "xai" | "deepseek" | "workers-ai"
  | "telegram"  // ← new

export interface TelegramCredential {
  botToken: string
  chatId: string  // captured from /start webhook
}

// Add validator:
export const TelegramCredentialSchema = z.object({
  botToken: z.string().min(1),
  chatId: z.string(), // empty until /start captured
})
```

Wire into the existing `saveCredential()` / `getCredential()` flow in `packages/data-ops/src/credential/`. The credential is encrypted at rest using the existing HKDF key derivation.

---

## Database Schema

### notification_settings table (Postgres)

Per-user notification preferences. Separate from the trading config to avoid bloating `user_trading_config`.

```ts
// packages/data-ops/src/drizzle/schema.ts (additions)

import { pgTable, text, timestamp, boolean } from "drizzle-orm/pg-core"
import { auth_user } from "./auth-schema"

export const notification_settings = pgTable("notification_settings", {
  userId: text("user_id")
    .primaryKey()
    .references(() => auth_user.id, { onDelete: "cascade" }),
  enableTradeProposals: boolean("enable_trade_proposals").notNull().default(true),
  enableTradeResults: boolean("enable_trade_results").notNull().default(true),
  enableDailySummary: boolean("enable_daily_summary").notNull().default(true),
  enableRiskAlerts: boolean("enable_risk_alerts").notNull().default(true),
  dailySummaryTime: text("daily_summary_time").notNull().default("17:00"),
  quietHoursStart: text("quiet_hours_start"), // "HH:mm" or null
  quietHoursEnd: text("quiet_hours_end"),     // "HH:mm" or null
  updatedAt: timestamp("updated_at").defaultNow().$onUpdate(() => new Date()).notNull(),
})

export type NotificationSettingsRecord = typeof notification_settings.$inferSelect
```

### Migration SQL

```sql
-- drizzle migration

CREATE TABLE notification_settings (
  user_id TEXT PRIMARY KEY REFERENCES auth_user(id) ON DELETE CASCADE,
  enable_trade_proposals BOOLEAN NOT NULL DEFAULT true,
  enable_trade_results BOOLEAN NOT NULL DEFAULT true,
  enable_daily_summary BOOLEAN NOT NULL DEFAULT true,
  enable_risk_alerts BOOLEAN NOT NULL DEFAULT true,
  daily_summary_time TEXT NOT NULL DEFAULT '17:00',
  quiet_hours_start TEXT,
  quiet_hours_end TEXT,
  updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);
```

### What we do NOT need

- **No `pending_approvals` table** — proposals already live in SessionAgent DO SQLite `trade_proposals`
- **No `approval_timeouts` table** — SessionAgent's `expireProposals()` already handles timeout
- **No `telegram_message_id` column** — store in SessionAgent SQLite if needed for message editing

### Optional: telegram_message_id in SessionAgent SQLite

If we want to edit Telegram messages after approval (remove buttons, update status), add a column to `trade_proposals`:

```sql
-- SessionAgent DO SQLite migration (tag: v7 or next)
ALTER TABLE trade_proposals ADD COLUMN telegram_message_id INTEGER;
```

---

## Zod Schemas

```ts
// packages/data-ops/src/zod-schema/telegram.ts

import { z } from "zod"

export const NotificationSettingsSchema = z.object({
  enableTradeProposals: z.boolean().optional(),
  enableTradeResults: z.boolean().optional(),
  enableDailySummary: z.boolean().optional(),
  enableRiskAlerts: z.boolean().optional(),
  dailySummaryTime: z.string().regex(/^\d{2}:\d{2}$/).optional(),
  quietHoursStart: z.string().regex(/^\d{2}:\d{2}$/).nullable().optional(),
  quietHoursEnd: z.string().regex(/^\d{2}:\d{2}$/).nullable().optional(),
})

export const TelegramWebhookUpdateSchema = z.object({
  update_id: z.number(),
  message: z.object({
    message_id: z.number(),
    from: z.object({ id: z.number(), username: z.string().optional() }),
    chat: z.object({ id: z.number(), type: z.string() }),
    text: z.string().optional(),
  }).optional(),
  callback_query: z.object({
    id: z.string(),
    from: z.object({ id: z.number(), username: z.string().optional() }),
    message: z.object({
      message_id: z.number(),
      chat: z.object({ id: z.number() }),
    }),
    data: z.string(),
  }).optional(),
})

export type NotificationSettingsInput = z.infer<typeof NotificationSettingsSchema>
```

---
