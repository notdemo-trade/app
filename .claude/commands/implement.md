Use dd-i agent and implement design doc: $ARGUMENTS

## Instructions

1. Find the design doc matching "$ARGUMENTS" in `docs/`
2. If ambiguous — list matches, ask which one
3. Read the full doc + its index fi exists (`docs/000-index.md`) for dependency context
4. Check dependency chain — confirm prerequisites are implemented (check codebase for existing code from prior docs)
5. Implement the COMPLETE spec from the doc using the dd-i agent
6. After implementation — summarize created/modified files