language js

pattern no_as_any() {
  `$expr as any` => . where {
    register_diagnostic(
      severity = "error",
      message = "Never cast to any. Use unknown and narrow with type guards."
    )
  }
}
